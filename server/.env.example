# --- Required ---
JWT_SECRET=d88d84afbb096967744f76ba27263171bfe9627f3a326033e1146cd7835b646169757036fc4729810a87e539a216342b

# Restrict allowed origins (comma-separated). Leave empty to allow all during staging.
CORS_ORIGINS=

# Public URL of this API (used in links inside emails, e.g., QR verify)
# Example: https://djscovery-api.onrender.com
API_BASE_URL=

# --- Firebase Admin (Firestore) ---
# One of the following should be provided for Firebase Admin credentials:
# 1) Path to a service account JSON file mounted in the container
# FIREBASE_SERVICE_ACCOUNT=/etc/secrets/service-account.json
# 2) GOOGLE_APPLICATION_CREDENTIALS works too
# GOOGLE_APPLICATION_CREDENTIALS=
# Project ID (optional if present in the service account JSON)
FIREBASE_PROJECT_ID=

# --- SMTP (email sending for bookings) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=no-reply@your-domain.tld

# --- 3rd party (optional; safe fallbacks exist) ---
# Spotify API (improves latest playlist fetching; fallback ID is used otherwise)
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_FALLBACK_PLAYLIST_ID=1pSy9kzEtp4El0Op5CV8pf
SPOTIFY_CACHE_TTL_MS=600000

# YouTube/Drive API (for widgets and Drive gallery proxy)
YOUTUBE_API_KEY=
GOOGLE_API_KEY=

# --- Newsletter (Brevo / Sendinblue) ---
# Obbligatori per abilitare l'iscrizione newsletter
BREVO_API_KEY=
BREVO_LIST_ID=
# Double Opt-In (DOI). Se true, servono anche TEMPLATE e REDIRECT
BREVO_USE_DOI=true
BREVO_DOI_TEMPLATE_ID=
BREVO_REDIRECT_URL=

# --- reCAPTCHA (opzionale) ---
RECAPTCHA_SECRET_KEY=
RECAPTCHA_MIN_SCORE=0.5

# --- Admin helpers (optional) ---
# Comma separated list of admin emails that receive admin role implicitly
ADMIN_EMAILS=
